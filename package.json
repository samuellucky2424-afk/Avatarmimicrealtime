{
  "name": "avatar-mimic-real-time-api",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@decartai/sdk": "^0.1.13",
    "@supabase/supabase-js": "^2.100.1",
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "build": "echo Backend API deploy",
    "electron": "npm --prefix app run electron",
    "electron:build": "npm --prefix app run electron:build",
    "electron:build:dir": "npm --prefix app run electron:build:dir",
    "electron:release": "npm --prefix app run electron:release"
  }
}
